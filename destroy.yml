---
- hosts: hostar
  tasks:
    - name: Execute command
      ansible.builtin.command:
        cmd: qm stop 1009 && sleep 5 && qm destroy 1009
      register: result
      failed_when: result.rc != 1

    - name: Display output
      ansible.builtin.debug:
        var: result.stdout_lines
